Create a dockefile  with a custom variable called ```workdir``` then
give it the desired path the change the working directory to it, then
copy a file to the docker image and set the destination to ```.```
